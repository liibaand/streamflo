// This is a placeholder file for proper project structure
// The main entry point is defined in package.json as "node_modules/expo/AppEntry.js"
import { registerRootComponent } from 'expo';
import App from './App';

registerRootComponent(App);
